import { PageLoadingState } from "@/components/loading-state";

export default function Loading() {
  return (
    <PageLoadingState
      eyebrow="Preparando a experiência"
      title="Montando o próximo passo"
      description="Carregando estrutura, contexto do usuário e blocos principais."
    />
  );
}
